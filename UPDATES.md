# ✅ Обновления: Генерация ID и улучшение карусели

## 🎯 Что сделано:

### 1. Генерация случайных номеров при входе

#### РНОКПП (10 цифр)
- ✅ Генерируется случайный номер из 10 цифр
- ✅ Обновляется при каждом входе в систему
- ✅ Отображается в карточке "Картка платника податків"

**Пример:** `4018401651`, `7293856104`, `1029384756`

#### Номер паспорта (9 цифр)
- ✅ Первая цифра: 0-3 (как в настоящих паспортах)
- ✅ Остальные 8 цифр: случайные
- ✅ Обновляется при каждом входе
- ✅ Отображается в карточке "Паспорт громадянина України"

**Пример:** `010322300`, `234567890`, `098765432`

### 2. Улучшена карусель документов

#### Что исправлено:
- ✅ **Лучшая видимость соседних карт** - теперь видны и предыдущая, и следующая карты
- ✅ **Увеличен минимальный scale** - с 0.85 до 0.90 (карты меньше уменьшаются)
- ✅ **Увеличена минимальная opacity** - с 0.60 до 0.75 (карты более яркие)
- ✅ **Уменьшен parallax эффект** - с 24px до 16px (меньше смещение)

#### Результат:
- При свайпе теперь хорошо видны обе соседние карты
- Плавная анимация перехода
- Лучшая навигация между документами

## 🔄 Как это работает:

### При входе в систему:
1. Пользователь вводит логин и пароль
2. Система генерирует случайные номера:
   - РНОКПП: 10 случайных цифр
   - Паспорт: первая цифра 0-3, затем 8 случайных цифр
3. Номера сохраняются в localStorage
4. При следующем входе генерируются новые номера

### При навигации по документам:
1. Свайп влево/вправо для переключения
2. Соседние карты видны и частично прозрачны
3. Активная карта в центре, полностью видима
4. Индикаторы внизу показывают текущую позицию

## 📱 Проверка:

1. Откройте: http://localhost:5174
2. Войдите в систему
3. Проверьте РНОКПП в карточке "Картка платника податків"
4. Проверьте номер паспорта в карточке "Паспорт"
5. Попробуйте свайпнуть между карточками - должны быть видны соседние карты

## 🔍 Технические детали:

### Генерация РНОКПП:
```javascript
const generateRNOKPP = () => {
    let rnokpp = '';
    for (let i = 0; i < 10; i++) {
        rnokpp += Math.floor(Math.random() * 10);
    }
    return rnokpp;
};
```

### Генерация номера паспорта:
```javascript
const generatePassportNumber = () => {
    const firstDigit = Math.floor(Math.random() * 4); // 0-3
    let passportNumber = firstDigit.toString();
    for (let i = 0; i < 8; i++) {
        passportNumber += Math.floor(Math.random() * 10);
    }
    return passportNumber;
};
```

### Параметры карусели:
- **Scale**: min 0.90 (было 0.85)
- **Opacity**: min 0.75 (было 0.60)
- **Parallax**: 16px (было 24px)

Все изменения применяются автоматически! 🎉
